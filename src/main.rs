fn main() {
    // variable-bindings
    let my_number = 0;
    let my_string = "hello string";

    let (first_var, second_var) = ("first_var", 2);

    let mut mutable_var = 1;
    mutable_var = 2;

    let integer_32_bit: i32 = 65537;

    print!("{}", integer_32_bit);
}